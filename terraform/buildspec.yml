version: 0.2
env:
  variables:
    DOCKER_BUILDKIT: 1
    AWS_ACCOUNT_ID: 966924987919
phases:
  install:
    runtime-versions:
      docker: 19
  pre_build:
    commands:
      - $(aws ecr get-login --no-include-email)
  build:
    commands:
      bash ./ci/build_and_push.sh